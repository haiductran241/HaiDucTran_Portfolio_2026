// Minimal reportWebVitals stub so CRA imports won't fail.
// Accepts an optional onPerfEntry function but does nothing by default.
const reportWebVitals = (onPerfEntry) => {
  if (onPerfEntry && typeof onPerfEntry === 'function') {
    // no-op: in a real app you could measure and send metrics here
  }
}

export default reportWebVitals
